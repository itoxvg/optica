.table-responsive
  %table.table.table-striped.table-hover.table-sm.table-vcenter
    %thead
      %tr
        %th código
        %th nombre
        %th estado
        %th existencias
        %th tipo
        %th actualizado el
        %th.text-center{:style => "width: 100px;"} Acciones
    %tbody
      - productos.each do |producto|
        %tr
          %td= producto.codigo
          %td= producto.nombre
          %td= estado_de_publicacion producto
          %td= producto.existencia
          %td= producto.tipo
          %td= l producto.updated_at, format: :long
          %td
            .btn-group
              = link_to producto, class: 'btn btn-alt-info' do
                %i.si.si-eye
                detalles
              = link_to [:edit, producto], class: 'btn btn-alt-warning' do
                %i.si.si-pencil
                editar
    %tfoot
      %tr
        %td{ colspan: 6 }
          .row
            .col-6
              = paginate productos
            .col-6
              .pull-right
                = page_entries_info productos
