- unless flash.empty?
  - if flash[:notice]
    .alerta.alert.alert-success.alert-dismissable{:role => "alert"}
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"} ×
      %p.mb-0= notice
  - if flash[:alert]
    .alerta.alert.alert-danger.alert-dismissable{:role => "alert"}
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"} ×
      %p.mb-0= alert
