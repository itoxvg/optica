<!DOCTYPE html>
<html>
  <head>
    <title>Optica</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

  </head>

  <body>
    <nav>
      <%= link_to "cerrar sesion", destroy_usuario_session_path %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
