<%= form_with(model: venta, local: true) do |form| %>
  <% if venta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venta.errors.count, "error") %> prohibited this venta from being saved:</h2>

      <ul>
      <% venta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :observaciones %>
    <%= form.text_area :observaciones %>
  </div>

  <div class="field">
    <%= form.label :saldada %>
    <%= form.check_box :saldada %>
  </div>

  <div class="field">
    <%= form.label :fecha_entrega %>
    <%= form.datetime_select :fecha_entrega %>
  </div>

  <div class="field">
    <%= form.label :descuento %>
    <%= form.text_field :descuento %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
